language: java

sudo: false

addons:
  apt:
    packages:
      - oracle-java8-installer

branches:
  only:
    - master

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
  - openjdk6

after_success:
  - mvn clean cobertura:cobertura coveralls:report -Pintegration
